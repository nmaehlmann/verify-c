int contains(Range r, int i){
    precondition("true");
    postcondition("((r.lb <= i && r.ub >= i) -> \result == 1) && (!(r.lb <= i && r.ub >= i) -> \result == 0)");
    
    if(r.lb <= i && i <= r.ub){
        return 1;
    }
    return 0;
}