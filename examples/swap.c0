void swap (int* x, int* y) {
    precondition("y != *x && x != *y && x != *x && y != *y && *x != *y && *x == X && *y == Y");
    postcondition("*x == Y && *y == X");

    int tmp = *x;
    *x = *y;
    *y = tmp;
    
    return;
}