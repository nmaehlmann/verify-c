int fib(int n) {
    precondition("n >= 0");
    postcondition("\result == fibonacci(n)");

    if(n == 0 || n == 1){
        return 1;
    } else {
        int prev = fib(n - 1);
        int prev2 = fib(n - 2);
        return prev + prev2;
    }
}